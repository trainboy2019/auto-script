language: bash
dist: trusty
sudo: false

addons:
  apt:
    sources:
    - debian-sid
    packages:
    - shellcheck

script:
- cp variables.conf.example variables.conf
- bash -c 'shellcheck -x cron-release.sh'
